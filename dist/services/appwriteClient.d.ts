import { Storage, Databases, ID, Query } from "node-appwrite";
export declare const storage: Storage;
export declare const databases: Databases;
export { ID, Query };
export declare const DB_CONFIG: {
    databaseId: string;
    tableId: {
        dat: string;
    };
};
